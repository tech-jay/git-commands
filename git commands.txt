yum install git -y
apt install git -y
git --version
git config --global user.name"jay"
git config --global user.email"jpg0389@gmail.com"
git config --list
git init
git pull origin mai
git push origin mai
git fetch
git add .
git status
git commit -m "First Commit"
git remote -v
git remote add origin <git repo>
git log
git show
git branch
git branch -m  <Branch name> To Create New Branch
git branch -D  <Branch name> To Delete  Branch
git ignore
git log --one line
git log -- grep "filename/fileformat"
git checkout <branch name> -- To Change the Branch name
git merge <Branch Name>
git rebase
git reset
git revert
git cleen -n/-f
git tag -a <name> -m "tag message" <Commit  id>
git clone
ssh-keygen
git cherry-pick <commit id>
git squash-
git stash-work with same branch with two different tasks. 

